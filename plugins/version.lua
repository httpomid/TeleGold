do

function run(msg, matches)
    return "🔱TeleGoldⓒ🔱 v1 Sudo > @omidhttp ✔ \n 🔱 @TeleGold_Team 🔱"
end 
return {
  patterns = {
    "^[/!#][vV]ersion$",
    "^[#!/][bB]ot$"
  },
  run = run
}
end
